[project]
name = "opencms"
version = "0.1.0"
description = "A minimal headless CMS for blog publishing, designed to integrate with hugo, zola, etc SSGs."
readme = "README.md"
requires-python = ">=3.8"
dependencies = []
